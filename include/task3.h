char* mixChars(char* in, char* out);
char* mixLine(char* instr, char* outstr);

#include <stdio.h>
#include <stdlib.h>

#define LINES 256
#define SYMBOLS_IN_LINE 256
#define NUMBER_OF_UNCHANGEABLE_CH 1
